{
  "name": "@mediafuse/sentry",
  "version": "0.1.0",
  "type": "module",
  "license": "BSD-2-Clause",
  "main": "dist/sentry.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/sentry.js"
  },
  "dependencies": {
    "@sentry/browser": "^9.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
